(function (window, undefined) {
    'use strict';

    var fn = {},
        _fn = {},
        document,
        console;

    document = window.document;
    console = window.console;

    fn.init = function () {
        _fn.init();
    };

    _fn.init = function () {

    };

    fn.init();
}(this));
